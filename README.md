# Hugo Baseline site

In Settings->Pages->Build and deployment, Source must be set to GitHub Actions, not Deploy from a branch since the payload is generated in a GitHub Action run, not stored in the repo itself.
